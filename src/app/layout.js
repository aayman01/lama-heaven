import "./globals.css";
import {dbConnect} from "../lib/mongoDb"
export const metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default async function RootLayout({ children }) {
  await dbConnect();
  return (
    <html lang="en">
      <body
        className={`antialiased font-popins`}
      >
        {children}
      </body>
    </html>
  );
}
